{
    "private": true,
    "name": "libsql-client",
    "devDependencies": {
    },
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
      "build": "yarn workspace @libsql/http-client run build && yarn workspace @libsql/client run build",
      "test": "yarn workspace @libsql/client test"
    }
  }
